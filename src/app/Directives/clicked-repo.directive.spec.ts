import { ClickedRepoDirective } from './clicked-repo.directive';

describe('ClickedRepoDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickedRepoDirective();
    expect(directive).toBeTruthy();
  });
});
