import { DaycreatedPipe } from './daycreated.pipe';

describe('DaycreatedPipe', () => {
  it('create an instance', () => {
    const pipe = new DaycreatedPipe();
    expect(pipe).toBeTruthy();
  });
});
